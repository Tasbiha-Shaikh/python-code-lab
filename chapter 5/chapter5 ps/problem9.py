# can you change a value inside a list which is contained in set s? s = {8,9,"harry",12,[1,2]}

s = {8,9,"harry",12,[1,2]}


# No — you cannot change a value inside a list that is inside a set.
# In fact, you cannot even put a list inside a set.